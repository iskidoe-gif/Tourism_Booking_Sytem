APP_NAME=TourismBooking
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://your-domain.com

LOG_CHANNEL=stack


DB_CONNECTION=pgsql
DB_HOST=dpg-d8gc0lflk1mc73enkdjg-a
DB_PORT=5432
DB_DATABASE=bolinaodb
DB_USERNAME=host
DB_PASSWORD=hxmfIvh6GNt6ZxVxoRkckSnLtCmjf2BL
CORS_ALLOW_ORIGIN=*

# When true the container will run `php artisan migrate --force` on startup (default true)
RUN_MIGRATIONS=true
# When true the container will run `php artisan db:seed --class=DatabaseSeeder --force` on startup
# Use this on Render free plan to auto-populate demo data. Set to false to disable.
RUN_SEEDS=false

CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailgun.org
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=null
MAIL_FROM_NAME="TourismBooking"
